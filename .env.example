RESEND_API_KEY=

# URL pública del sitio, sin barra final (ej. https://ejemplo.com). Opcional:
# sin esta variable, el sitio usa la URL de producción o de deployment que
# provee Vercel automáticamente, o localhost en desarrollo (ver
# src/lib/site-url.ts). El dominio candidato (valinoragency.com.ar) todavía
# no está comprado ni activo — no asignarlo acá hasta confirmarlo.
SITE_URL=
